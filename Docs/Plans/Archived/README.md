# Archived Plans

Completed and cancelled execution plans live here for historical reference.
Before moving a plan, set its front-matter `status` to `complete` or
`cancelled`, finish the verification notes, and fold any durable product or
platform rules into their canonical documentation owner. Do not reactivate an
archived plan; create a new active plan under `Docs/Plans/` when follow-up work
is needed.
