import TrinketContentTestSupport

/// Re-exported from `TrinketContentTestSupport` (the single home for combat
/// fixtures) so existing suites keep working. New code should import
/// `TrinketContentTestSupport` directly.
public typealias CombatantFixtures = TrinketContentTestSupport.CombatantFixtures
