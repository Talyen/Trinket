# TrinketBattleRuntime-local guide

Play-owned rewards/routes stay in AppState contracts; presentation stays in `TrinketBattleFeature`. Ownership, import bans (enforced by `check-module-boundaries.sh`), and testing: [README.md](README.md).
