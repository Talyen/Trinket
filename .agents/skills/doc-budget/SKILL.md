---
name: doc-budget
description: Auto-triggers when authoring or modifying Swift comments. Applies the repository comment guardrail and checker rules from AGENTS.md and Scripts/check-comment-ban.sh.
---

# Comment hygiene

Activate when: authoring or modifying Swift comments.

Swift comments are banned in authored sources (`SWIFT_SOURCE_DIRS` / `Packages/*`).
`Scripts/check-comment-ban.sh`, `Scripts/check-agent-invariants.sh`, and `banned_comments`
in `.swiftlint.yml` enforce this as part of `Scripts/test.sh style` and
`.githooks/pre-push`. Allowed toolchain
directives only: `// swift-tools-version:`, `// swiftlint:disable|enable` / `// swiftformat:disable|enable`
(every disable requires ` - <reason>`), and `// Generated by` headers in
`Packages/TrinketContent/Sources/TrinketContent/Generated/**`.
Transitional hatches `// *Check: allow - <reason>` and `// Concurrency-Safety:` remain
temporarily exempt. Encode rationale in names, types, tests, or invariant checks instead of comments.
