# Single source for managed simulator names shared by shell and Python.
# Shell: source this file or read the values via Scripts/lib/simctl.sh.
# Python: Scripts/simctl_json.py loads this file (see MANAGED_SIMULATOR_NAMES).
TRINKET_SHARED_SIM_NAMES=("Trinket Run" "Trinket CI")
TRINKET_AGENT_SIM_PATTERN='^Trinket Agent [0-9]+$'
